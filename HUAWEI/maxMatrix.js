function maxMatrix() {
}

function findMaxSum(matrix, startRow, startCol, maxRow, maxCol) {
    let max = 0;
    let row = startRow;
    let col = startCol;
    if (row < maxRow && col < maxCol) {
        let sum = 0;
        for(let i = startRow; i < maxRow;i++) {
            sum
        }
    }
}